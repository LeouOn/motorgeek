data/*.db
config.yaml
# DB is NOT tracked — schema lives in Alembic migrations, seed data in seed_data.py.
# After cloning: run `motorgeek serve` once (auto-creates tables via init_db + Alembic),
# then `python seed_data.py` for sample cars. Run `python scripts/load_data.py` to
# restore from a previously dumped seed_data.json if available.
__pycache__/
*.pyc
.pytest_cache/
.coverage
.ruff_cache/
.env
*.egg-info/
dist/
build/